# Prerequisites
*.d

# Compiled Object files
*.so
!libonnxruntime.so
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Executables
*.exe
*.out
*.app

# Build dir
build/

# Build objs
obj/

# Editors
.vscode/
.vscode
*.code-workspace
.claude/
*.swp
*~

# CMake
**/CMakeCache.txt
**/CMakeFiles/
**/cmake_install.cmake

# OS
.DS_Store
Thumbs.db

# Temporary local folders (a trailing slash would only match real
# directories, not symlinks -- some of these, like _dafx26_projects,
# are symlinks into an unrelated repo)
/_*

# neural models that need author's permission
*.nam

# Keep QNN headers out of git, since we're not allowed to distribute them
dependencies/QNN/include/**
# but keep the (otherwise empty) folder versioned so the SDK has a place to go
!dependencies/QNN/include/.gitkeep
